
   
name: CI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2.4.0

      - name: Install deps
        run: yarn
      
      - name: Build
        run: yarn build
      
      - name: Deploy
        run: yarn deploy